services:
  backend:
    build: ./backend
    image: d2d-api
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
  backend:
    build: ./frontend
    image: d2d-front
    ports:
      - "80:80"
